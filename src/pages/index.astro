---
import { getCollection } from 'astro:content';
const posts = (await getCollection('blog')).sort((a, b) => (b.data.pubDate || '').localeCompare(a.data.pubDate || ''));
const base = (import.meta.env.PUBLIC_SITE_URL || Astro.site || 'https://<your-site>.netlify.app').toString().replace(/\/$/, '');
const pageUrl = new URL(Astro.url.pathname, base).toString();

// タグとカテゴリの統計を計算
const allTags = new Set();
const allCategories = new Set();
posts.forEach(post => {
  post.data.tags?.forEach(tag => allTags.add(tag));
  if (post.data.category) allCategories.add(post.data.category);
});
---
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <title>IELTS 学習ガイド - 総合的な英語力向上のためのリソース</title>
    <meta name="description" content="IELTS Writing Task 2を中心とした包括的な英語学習ガイド。実践的なテクニック、表現集、学習法を提供します。" />
    <link rel="canonical" href={pageUrl} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { 
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
        line-height: 1.6; 
        color: #333; 
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
      }
      .container { 
        max-width: 1200px; 
        margin: 0 auto; 
        padding: 2rem 1rem; 
      }
      .header {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 3rem 2rem;
        margin-bottom: 2rem;
        text-align: center;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      }
      .header h1 {
        font-size: 3rem;
        font-weight: 700;
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 1rem;
      }
      .header p {
        font-size: 1.2rem;
        color: #666;
        margin-bottom: 2rem;
      }
      .stats {
        display: flex;
        justify-content: center;
        gap: 2rem;
        flex-wrap: wrap;
      }
      .stat-item {
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
        padding: 1rem 2rem;
        border-radius: 50px;
        font-weight: 600;
        box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
      }
      .nav-links {
        display: flex;
        justify-content: center;
        gap: 1rem;
        margin-top: 2rem;
      }
      .nav-link {
        background: rgba(255, 255, 255, 0.9);
        color: #667eea;
        padding: 0.8rem 2rem;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }
      .nav-link:hover {
        background: #667eea;
        color: white;
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
      }
      .content-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 2rem;
        margin-top: 2rem;
      }
      .post-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 15px;
        padding: 2rem;
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.2);
      }
      .post-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
      }
      .post-title {
        font-size: 1.3rem;
        font-weight: 700;
        margin-bottom: 1rem;
        line-height: 1.4;
      }
      .post-title a {
        color: #333;
        text-decoration: none;
        transition: color 0.3s ease;
      }
      .post-title a:hover {
        color: #667eea;
      }
      .post-meta {
        color: #666;
        font-size: 0.9rem;
        margin-bottom: 1rem;
        display: flex;
        align-items: center;
        gap: 1rem;
      }
      .post-description {
        color: #555;
        line-height: 1.6;
        margin-bottom: 1.5rem;
      }
      .post-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
      }
      .tag {
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
        padding: 0.3rem 0.8rem;
        border-radius: 20px;
        font-size: 0.8rem;
        font-weight: 500;
      }
      .empty-state {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 3rem 2rem;
        text-align: center;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      }
      .empty-state h2 {
        color: #667eea;
        margin-bottom: 1rem;
        font-size: 2rem;
      }
      .empty-state p {
        color: #666;
        font-size: 1.1rem;
        line-height: 1.6;
      }
      @media (max-width: 768px) {
        .header h1 { font-size: 2rem; }
        .stats { gap: 1rem; }
        .stat-item { padding: 0.8rem 1.5rem; }
        .nav-links { flex-direction: column; align-items: center; }
        .content-grid { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header class="header">
        <h1>IELTS 学習ガイド</h1>
        <p>総合的な英語力向上のためのリソース</p>
        <div class="stats">
          <div class="stat-item">
            📚 {posts.length} 記事
          </div>
          <div class="stat-item">
            🏷️ {allTags.size} タグ
          </div>
          <div class="stat-item">
            📁 {allCategories.size} カテゴリ
          </div>
        </div>
        <div class="nav-links">
          <a href="/tags/" class="nav-link">タグ一覧</a>
          <a href="/categories/" class="nav-link">カテゴリ一覧</a>
        </div>
      </header>

      {posts.length === 0 ? (
        <div class="empty-state">
          <h2>📝 記事を準備中</h2>
          <p>
            現在、記事を準備しています。<br>
            まもなく充実したコンテンツをお届けします。
          </p>
        </div>
      ) : (
        <div class="content-grid">
          {posts.map(post => (
            <article class="post-card">
              <h2 class="post-title">
                <a href={`/blog/${post.slug}/`}>{post.data.title}</a>
              </h2>
              <div class="post-meta">
                {post.data.pubDate && (
                  <span>📅 {post.data.pubDate}</span>
                )}
                {post.data.category && (
                  <span>📁 {post.data.category}</span>
                )}
              </div>
              {post.data.description && (
                <p class="post-description">{post.data.description}</p>
              )}
              {post.data.tags && post.data.tags.length > 0 && (
                <div class="post-tags">
                  {post.data.tags.map(tag => (
                    <span class="tag">#{tag}</span>
                  ))}
                </div>
              )}
            </article>
          ))}
        </div>
      )}
    </div>
  </body>
</html>
